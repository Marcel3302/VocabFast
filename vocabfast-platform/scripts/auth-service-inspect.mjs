const {CLOUDFLARE_API_TOKEN:token,CLOUDFLARE_ACCOUNT_ID:account}=process.env;
for(const path of ['r2/buckets','workers/scripts/vocabfast-language-preview/settings']){const response=await fetch(`https://api.cloudflare.com/client/v4/accounts/${account}/${path}`,{headers:{Authorization:`Bearer ${token}`}});const data=await response.json();if(!response.ok||!data.success)throw new Error('Could not inspect auth configuration.');console.log(path,JSON.stringify(path==='r2/buckets'?data.result.buckets.map(b=>({name:b.name})):data.result.bindings.map(b=>({name:b.name,type:b.type,bucket_name:b.bucket_name}))));}
